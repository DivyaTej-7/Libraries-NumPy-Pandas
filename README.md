# Libraries-NumPy-Pandas
* Loads retail dataset using Pandas * Cleans data by handling missing values * Computes Revenue = Quantity × Price * Performs grouping by Category &amp; Region * Identifies top &amp; bottom performers * Extracts month from Date for time analysis * Calculates total, mean, median &amp; std revenue using NumPy
